name: Rust

on:
  push:
    branches: [ "main", "cli" ]
  pull_request:
    branches: [ "main", "cli" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: cargo build --verbose
